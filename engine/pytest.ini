[pytest]
testpaths = tests
markers =
    slow: makes real `claude` calls against the reference deck; costs money and takes minutes
# Slow tests are excluded by default. Run them explicitly with `-m slow`.
addopts = -m "not slow"
